name: Build APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 🧾 Checkout code
      uses: actions/checkout@v3

    - name: 🐍 Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.10

    - name: 📦 Install dependencies
      run: |
        sudo apt update
        sudo apt install -y \
          build-essential ccache git zip unzip \
          openjdk-17-jdk python3-pip python3-setuptools \
          python3-wheel libncurses5 libstdc++6 zlib1g-dev \
          libffi-dev libssl-dev libsqlite3-dev libjpeg-dev \
          autoconf libtool pkg-config

        pip install --upgrade pip
        pip install buildozer cython

    - name: 🔧 Build APK
      run: |
        buildozer -v android debug

    - name: 📤 Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: GPT_MEXC_Trader_APK
        path: bin/*.apk
